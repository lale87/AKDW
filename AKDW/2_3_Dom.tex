%---------------------------------- 
\subsection{DOM-Basiert (lokal)}
%----------------------------------
Wie auch beim reflektierten XSS wird beim DOM-Basierten XSS ein Parameter in der
 Url genutzt, um ein Script beim Anwender zur Ausführung zu bringen. Im Gegensatz zum 
 reflektierten XSS wird das Script allerdings nicht an den Server gesendet, sondern direkt 
 beim Anwender ausgeführt, es findet also nur lokal statt. Der Angriff erfolgt i.d.R. 
 ebenfalls über einen manipulierten Link.