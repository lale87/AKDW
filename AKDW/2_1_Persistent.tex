%---------------------------------- 
\subsection{Persistent}
%---------------------------------- 
Beim persistenten XSS wird das Script durch den Angreifer auf einen Webserver
übertragen und dort persistiert. Durch diese dauerhafte Speicherung des Schadcodes 
wird das Script bei jedem Seitenaufruf direkt von dem Webserver an die Clients 
ausgeliefert. Ein gutes Beispiel hierfür ist das Übertragen von Schadcode in Foren. 
Durch das Einbinden von Scripten in Kommentare, wird dieses auf den Server übertragen 
von dort an jeden Leser wieder ausgeliefert. Es Bedarf also keiner aktiven Tätigkeit 
durch den Anwender mit Ausnahme des Besuchs der vermeintlich sicheren Seite.